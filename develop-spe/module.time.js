const
    util    = require('./module.time.util.js'),
    model   = require('./module.time.model.js'),
    factory = require('./module.time.factory.js');

exports.model   = Object.freeze(model);
exports.factory = Object.freeze(factory);
